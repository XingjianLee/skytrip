"""SkyTrip backend application package initializer."""

# This file marks the 'app' directory as a Python package so that
# imports like 'from app import dependencies' work correctly when
# running under Uvicorn.